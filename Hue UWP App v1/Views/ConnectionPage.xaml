<Page
    x:Class="TC.HueApplication.Views.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TC.HueApplication.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid>
    <Frame x:Name="ConnectionPageFrame" FontFamily="Segoe UI">
      <RelativePanel HorizontalAlignment="Center" VerticalAlignment="Center" Height="393" Margin="0,0,0,0">
        <TextBlock x:Name="BridgeCountBlock" HorizontalAlignment="Center" Text="There were ## bridge(s) found" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="48" FontFamily="Segoe UI"/>
        <ListBox x:Name="BridgeListBox" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,150,0,0" ScrollViewer.HorizontalScrollBarVisibility="Auto" Width="200" RelativePanel.AlignHorizontalCenterWithPanel="True"/>
        <Button x:Name="ConnectButton" Content="Connect" HorizontalAlignment="Center" VerticalAlignment="Top" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="" RelativePanel.AlignBottomWithPanel="True" Height="62" Width="123"/>

      </RelativePanel>
    </Frame>
  </Grid>
</Page>
